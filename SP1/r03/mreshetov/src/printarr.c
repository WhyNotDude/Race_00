#include "header.h"

void printarr(t_screen * screen){

    for (int i = 0; i < screen->scale->y; i++){
        for (int j = 0; j < screen->scale->x; j++)
        {
            printf("lol");
        }
        
    }
}
